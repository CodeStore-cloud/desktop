package cloud.codestore.client.ui;

/**
 * An event indicating that the UI is initialized and the client is connected to the {CodeStore} core and therefore,
 * the application is fully ready.
 */
public record ApplicationReadyEvent() {}
