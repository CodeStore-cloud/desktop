package cloud.codestore.client.ui.selection.filter;

import cloud.codestore.client.Language;

/**
 * An event to add filter values via quickfilter buttons.
 */
public record QuickFilterEvent(Language language) {}
